server:
  port: 8086

spring:
  application:
    name: notification-service
management:
  tracing:
    sampling:
      probability: 1.0

management.zipkin.tracing.endpoint: http://localhost:9411/api/v2/spans